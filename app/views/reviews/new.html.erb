<div class="container">
  <h1>Review <%= @restaurant.name.capitalize! %></h1>
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit class:"btn-success"%>
  <% end %>
</div>
