<div class="container">
  <h1>Restaurants</h1>
  <ul>
    <% @restaurants.each do |restaurant| %>
    <li class="card">
      <li><%= link_to "#{restaurant.name.capitalize!}", restaurant_path(restaurant) %></li>
      <li><%= restaurant.category %></li>
    </li>
    <% end %>
  </ul>
</div>
